import React from 'react';
import ReactDOM from 'react-dom';

//This is where we will define the JSX for our react app. 
//All components and their logic can go in here.
//This is separate from the node server. 
//The node server is the back end. This is the front end. 
//So, buttons, webpage structure, event listeners to make the 
//buttons do stuff, ect
function App () {
    const 
}

ReactDOM.render(<App/>,document.getElementById('root'))